# CLAUDE.md

# 社区养老服务中心照护管理系统 —— AI 开发总规则

> 本文件是本项目的 AI 编程总规则，适用于 Cursor、Claude Code、Codex、Copilot 等 Coding Agent。
> 如果项目当前代码与本文档存在冲突，AI 必须优先保护已有可运行功能、数据完整性和安全性，并明确说明冲突。

---

## 1. 项目定位

项目名称：

**社区养老服务中心照护管理系统的设计与实现**

项目性质：

- 本科毕业设计
- Web 管理端 + 微信小程序家属端
- 面向社区养老服务中心日常照护业务
- 必须能够真实运行、真实落库、真实联调、真实演示

核心目标：

> 通过信息化手段统一管理老人档案、健康记录、异常预警、照护服务、人员排班、家属协同、餐饮、活动、药品提醒、权限日志和统计报表。

本项目不是单纯的后台 UI 模板，也不是 Demo 页面集合。

---

# 2. AI 的最高原则

任何编码任务必须遵循：

1. 先理解现有代码，再修改。
2. 先定位影响范围，再决定修改方案。
3. 优先复用现有架构、组件、工具类和接口规范。
4. 采用最小修改原则。
5. 核心业务必须真实落库。
6. 禁止 Mock 核心业务数据。
7. 禁止假按钮、假接口、假统计、假成功。
8. 权限必须后端校验，前端隐藏只是体验层。
9. 家属端必须执行严格的数据隔离。
10. 不得擅自删除已有正常功能。
11. 不得为了“高级”而无意义增加技术栈。
12. 不得虚构已经完成、已经测试或已经部署的功能。
13. 数据库、API、前端、论文必须保持一致。
14. 不确定的核心业务规则不得自行编造。
15. 所有重要修改完成后必须进行自检。

---

# 3. 技术栈

默认技术栈：

## 后端

- Java
- Spring Boot
- Spring MVC
- MyBatis / MyBatis-Plus
- MySQL
- Redis
- JWT
- Maven

## Web 管理端

- Vue 3
- Vite
- Element Plus
- Axios
- Vue Router
- Pinia
- ECharts

## 家属端

- 微信小程序

除非用户明确要求，不得随意替换核心技术栈。

禁止为了毕业设计“堆技术”而无必要引入：

- 微服务
- Kubernetes
- Elasticsearch
- Kafka
- RabbitMQ
- Docker 全套部署
- 复杂工作流引擎
- 不必要的第三方 SaaS

---

# 4. AI 开始任务前必须检查

## 后端任务

至少检查：

- pom.xml
- application.yml
- application-*.yml
- 启动类
- 安全配置
- JWT 配置
- 统一返回结构
- 全局异常处理
- Entity
- DTO
- VO
- Mapper
- Service
- Controller

## 前端任务

至少检查：

- package.json
- router
- stores
- api
- views
- components
- layouts
- Axios 封装
- 登录状态
- 权限控制

## 数据库任务

至少检查：

- SQL 文件
- 当前表结构
- Entity
- Mapper
- Service
- Controller
- 使用该数据的前端页面

---

# 5. 强制开发流程

所有重要功能统一按照：

```text
需求理解
  ↓
检查现有代码
  ↓
确认数据库模型
  ↓
确认业务规则
  ↓
设计 API
  ↓
实现后端
  ↓
后端编译/测试
  ↓
实现前端
  ↓
前后端联调
  ↓
权限检查
  ↓
异常流程检查
  ↓
重复操作检查
  ↓
最终自检
```

禁止：

```text
不看项目 → 直接大量生成新代码
```

---

# 6. 绝对禁止 Mock

正式业务代码禁止：

```javascript
const elders = [...]
Math.random()
setTimeout(() => resolve(...))
fakeData
mockData
demoData
```

禁止用写死数字代替数据库统计。

例如禁止：

```javascript
elderCount = 128
serviceCount = 56
warningCount = 9
```

正式页面必须：

```text
数据库
 ↓
后端 API
 ↓
前端
```

测试环境允许使用测试数据，但必须真实写入数据库，并明确属于测试/演示数据。

---

# 7. 禁止假功能

所有正式按钮必须对应真实业务：

- 新增必须真实新增
- 修改必须真实修改
- 删除必须真实删除/逻辑删除
- 查询必须真实查询
- 签到必须产生签到记录
- 报名必须产生报名记录
- 评价必须产生评价记录
- 预警必须产生预警记录
- 调班必须改变排班状态
- 药品提醒必须有真实提醒记录/任务

不能通过：

```text
按钮点击
 ↓
前端直接显示“成功”
```

冒充完成。

---

# 8. 最小修改原则

如果用户只要求增加一个筛选条件，只修改实现该功能所需要的范围。

禁止借机：

- 重写整个模块
- 更换 UI 框架
- 更换 ORM
- 重写登录
- 重写权限
- 重构所有 API
- 删除旧页面

大范围重构必须先说明：

- 原因
- 影响
- 风险
- 替代方案

---

# 9. 数据库优先

复杂业务必须先明确：

```text
业务对象
 ↓
实体关系
 ↓
数据库
 ↓
API
 ↓
前端
```

不要先做漂亮页面，再临时拼数据库。

---

# 10. 核心业务模块

必须围绕以下 12 个模块开发：

1. 老人档案管理
2. 健康记录管理
3. 健康异常预警
4. 照护服务项目管理
5. 照护人员排班
6. 服务记录与评价
7. 家属信息查看
8. 餐饮管理
9. 活动管理
10. 药品管理与提醒
11. 系统权限与日志
12. 统计报表

---

# 11. 三类核心角色

默认角色：

```text
ADMIN        管理员
CARE_STAFF   照护人员
FAMILY       家属
```

权限必须遵循：

```text
用户
 ↓
角色
 ↓
权限
 ↓
业务数据
```

后端必须是最终安全边界。

---

# 12. 家属数据隔离

家属查询老人数据时，不能只相信请求中的 elderId。

必须验证：

```text
当前登录用户
 ↓
家属身份
 ↓
老人绑定关系
 ↓
目标老人
 ↓
是否允许访问
```

禁止：

```text
GET /api/elders/123
```

仅凭 URL 中的 123 返回数据。

尤其保护：

- 健康记录
- 用药信息
- 联系方式
- 服务记录
- 评价
- 家庭关系

---

# 13. 健康数据规则

至少支持：

- 血压
- 血糖
- 体温
- 心率

健康记录应具备：

- elder_id
- 指标值
- 测量时间
- 录入人员
- 备注

血压建议拆为：

- systolic_pressure
- diastolic_pressure

健康预警必须形成完整闭环：

```text
健康数据录入
 ↓
参数校验
 ↓
读取规则
 ↓
判断异常
 ↓
保存健康记录
 ↓
异常则生成预警
 ↓
通知/待处理
 ↓
照护人员处理
 ↓
记录处理结果
```

系统属于健康管理辅助系统，不得宣称进行医疗诊断。

阈值必须统一配置，不得在不同 Service 中散落硬编码。

---

# 14. 服务业务闭环

```text
服务项目
 ↓
预约
 ↓
时间/人员检查
 ↓
排班
 ↓
服务执行
 ↓
签到
 ↓
服务记录
 ↓
完成
 ↓
评价
```

每个服务功能都必须明确处于哪个业务阶段。

---

# 15. 排班

后端必须检查：

- 同一照护人员时间冲突
- 调班冲突
- 无效人员
- 无效时间
- 已取消班次

不能只依赖前端检查。

---

# 16. 活动

完整流程：

```text
创建
 ↓
发布
 ↓
报名
 ↓
签到
 ↓
活动记录
```

必须处理：

- 重复报名
- 人数上限
- 已结束活动
- 重复签到
- 活动取消

---

# 17. 餐饮

支持：

- 每周食谱
- 早餐/午餐/晚餐
- 老人订餐
- 特殊饮食
- 忌口
- 过敏信息

特殊饮食信息必须与老人实际档案合理关联。

---

# 18. 药品提醒

支持：

- 药品
- 剂量
- 用法
- 频次
- 开始时间
- 结束时间
- 提醒时间
- 执行/提醒记录

Redis 可用于提醒任务辅助，但不能把 Redis 当作最终业务数据库。

---

# 19. 日志

重要操作必须记录：

- 登录
- 老人新增/修改/删除
- 健康数据录入
- 预警处理
- 服务预约
- 签到
- 排班
- 调班
- 活动发布
- 权限变更
- 用户状态变更

日志至少包含：

- 操作用户
- 模块
- 操作类型
- 时间
- 结果

禁止记录密码、JWT 和不必要的敏感健康信息。

---

# 20. API 规范

推荐：

```text
GET    /api/elders
GET    /api/elders/{id}
POST   /api/elders
PUT    /api/elders/{id}
DELETE /api/elders/{id}
```

避免：

```text
/getAllElder
/getElderList2
/test
/aaa
```

必须遵循项目已有 API 风格。

---

# 21. DTO / VO

推荐：

```text
Entity = 数据库模型
DTO    = 请求参数
VO     = 返回数据
```

禁止无脑直接返回 Entity。

敏感字段不得返回给不需要的角色。

---

# 22. 错误处理

统一处理：

- 参数错误
- 数据不存在
- 权限不足
- 业务冲突
- Token 异常
- 数据库异常

推荐使用：

```text
GlobalExceptionHandler
```

用户看到的是业务友好提示，而不是：

```text
NullPointerException
SQL Exception
Stack Trace
```

---

# 23. 防重复操作

重点检查：

- 服务预约
- 活动报名
- 活动签到
- 服务签到
- 服务评价
- 调班申请

前端负责 loading，后端负责最终幂等/唯一性保护。

---

# 24. 状态机

涉及状态的业务必须定义合法状态流转。

例如：

```text
待执行
 ↓
已分配
 ↓
服务中
 ↓
已完成
```

禁止无业务依据地从终态回退。

---

# 25. Redis

合理用途：

- 热点缓存
- 未读预警数量
- 消息辅助
- 药品提醒
- 定时任务辅助

修改数据库后必须考虑缓存失效。

---

# 26. 配置

禁止把以下信息硬编码到业务代码：

- 数据库地址
- 数据库密码
- Redis 密码
- JWT 密钥
- API 地址

使用配置文件或环境变量。

---

# 27. 安全

敏感信息包括：

- 身份证号
- 手机号
- 健康信息
- 用药信息
- 家属联系方式

需要按实际业务进行访问控制和合理脱敏。

密码只能保存哈希值。

禁止：

```text
明文密码
console.log(password)
```

---

# 28. SQL

禁止 SQL 字符串拼接用户输入。

必须使用参数化查询。

正式接口尽量不要无意义使用：

```sql
SELECT *
```

分页、索引、条件和排序必须合理。

---

# 29. 删除

以下历史数据一般不直接物理删除：

- 健康记录
- 服务记录
- 签到记录
- 评价
- 预警
- 操作日志

优先使用状态、逻辑删除或归档。

---

# 30. 前端

标准管理页：

```text
查询区
 ↓
操作区
 ↓
表格
 ↓
分页
```

所有异步操作必须有：

- loading
- 成功提示
- 错误提示
- 空数据状态

删除必须二次确认。

---

# 31. 统计

统计必须来源于真实业务表。

例如：

```text
服务总量
 =
真实服务记录 COUNT
```

ECharts：

- 健康趋势 → 折线图
- 服务量 → 柱状图
- 护理等级 → 饼图
- 活动报名 → 柱状图
- 趋势 → 折线图

禁止随机图表。

---

# 32. 编译和测试

修改后根据项目实际脚本执行：

```bash
mvn test
mvn package
npm run build
npm run lint
```

不要为了“通过检查”而关闭检查、删除测试或注释错误代码。

如果没有执行测试，不得声称测试通过。

---

# 33. 任务完成标准

模块只有在以下内容基本完成后才能标记 DONE：

```text
数据库
Entity
DTO/VO
Mapper
Service
Controller
API
前端
新增
修改
查询
删除/状态
分页
搜索
权限
异常
空状态
成功提示
错误提示
测试
```

---

# 34. AI 最终自检

每次重要任务结束前必须检查：

```text
真实吗？
是否真实落库？
权限正确吗？
家属会不会越权？
有没有 Mock？
有没有写死统计数字？
有没有破坏旧功能？
异常处理了吗？
重复操作处理了吗？
前后端联调了吗？
论文能否真实描述？
答辩能否演示？
```

---

# 35. 禁止事项

```text
❌ Mock 核心业务数据
❌ 随机业务数据
❌ 假接口
❌ 假按钮
❌ 假成功
❌ 写死 KPI
❌ 前端单独权限
❌ 家属越权
❌ 明文密码
❌ SQL 拼接
❌ 随意 DROP TABLE
❌ 随意 TRUNCATE
❌ 随意删除旧功能
❌ 无意义重构
❌ 无意义堆技术
❌ 虚构测试结果
❌ 虚构性能数据
❌ 虚构论文内容
```

---

# 36. 一句话总规则

> **先理解，再最小修改；真实数据、真实业务、真实权限、真实联调；任何功能都必须可运行、可测试、可解释，并与毕业论文保持一致。**
